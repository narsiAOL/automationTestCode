version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/html/familytree-web
    overwrite: true

file_exists_behavior: OVERWRITE

hooks:
  AfterInstall:
    - location: deploy.sh
      runas: root
